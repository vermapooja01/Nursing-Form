.table-responsive
  %table.table.table-hover
    %thead
      %tr
        %th{:align => 'center'}
          Patient Initials

        %th{:scope => 'col'}
          Age

        %th{:scope => 'col'}
          Gender

        %th{:scope => 'col'}
          Admitting Primary Medical Diagnosis

        %th{:scope => 'col'}
          Student

        %th{:scope => 'col'}
          Date

        %th


    %tbody
      - @finished_submissions.each do |submission|
        %tr
          %td= submission.patient_initials
          %td= submission.age
          %td= submission.gender
          %td= submission.admitting_primary_medical_diagnosis
          %td= submission.user.first_name
          %td= submission.week.strftime("%b %d, %Y")
          %td= link_to 'Show', submission, :class => "btn btn-default btn-xs"
